# iron-core-cluster-v2 协作硬性标准

## 依赖管理

- `Cargo.toml` 里添加独立第三方依赖，不使用 workspace 依赖。

## 注释规范

- Rust 代码统一使用 `//` 注释，不使用 `///` 或 `//!`。
- 每个数据结构上方、每个字段后方、每个方法上方都必须有中文注释。

## 变更确认

- 涉及数据结构或方法变更时，Codex/AI 必须先用表格说明变更了数据结构和方法，用户确认后再编码。
